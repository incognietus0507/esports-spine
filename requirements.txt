# Core
httpx>=0.27
pydantic>=2.6
python-dotenv>=1.0
structlog>=24.1
APScheduler>=3.10

# Parsing (compliant sources)
feedparser>=6.0
selectolax>=0.3

# Headless browser — only needed for the (disabled) JS-rendered sources.
# Install separately if you enable one: pip install playwright && playwright install chromium
# playwright>=1.43
